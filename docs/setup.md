# Dev Environment Setup

Notes on my local setup — so I can reproduce it or hand it off.

## Machine

- OS: 
- Shell: 
- Editor: 

## Tools Installed

- [ ] Node (version: )
- [ ] Python (version: )
- [ ] `uv` (Python package manager)
- [ ] Claude Code: `npm install -g @anthropic-ai/claude-code`
- [ ] Git

## Common Commands

```bash
# Run Claude Code in a project
cd projects/my-project
claude

# Start a new project from template
cp -r projects/example-project projects/new-name
```

## Environment Variables

Any secrets or API keys I use across projects — store in a password manager, not here.

| Variable | What it's for |
|----------|--------------|
| `ANTHROPIC_API_KEY` | Claude API |
